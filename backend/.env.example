PORT=3000
UPLOAD_DIR=./uploads
OUTPUT_DIR=./outputs
MAX_FILE_SIZE_MB=5000
DEFAULT_CLIP_DURATION=45

# Set to true on the laptop where Whisper is installed.
WHISPER_ENABLED=false
WHISPER_MODEL=large-v3
WHISPER_CLIP_MODEL=large-v3

# FFmpeg rendering knobs.
FFMPEG_PRESET=fast
FFMPEG_CRF=22
