{
  "name": "video-splitter-for-shorts",
  "version": "1.0.0",
  "description": "Upload a video and automatically split it into vertical short clips ready for YouTube Shorts, Instagram Reels, and TikTok.",
  "private": true,
  "scripts": {
    "install:all": "cd backend && npm install && cd ../frontend && npm install",
    "start:backend": "cd backend && node server.js",
    "start:frontend": "cd frontend && npx ng serve --port 4200 --open",
    "dev": "concurrently \"npm run start:backend\" \"npm run start:frontend\"",
    "dev:backend": "cd backend && npm run dev",
    "dev:frontend": "cd frontend && npx ng serve --port 4200 --open"
  },
  "devDependencies": {
    "concurrently": "^9.1.0"
  }
}
